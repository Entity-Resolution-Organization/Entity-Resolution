# ============================================================
#  Inference Pipeline — Python Dependencies
# ============================================================

# --- API & Web ---
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
python-multipart==0.0.9         # file upload for batch endpoint
httpx==0.27.0                   # async HTTP client

# --- Streamlit UI ---
streamlit==1.35.0
plotly==5.22.0                  # charts + gauges in UI
pandas==2.2.2

# --- GCP ---
google-cloud-storage==2.16.0
google-cloud-aiplatform==1.55.0  # Vertex AI client
google-auth==2.29.0

# --- ML / NLP ---
torch==2.2.2
transformers==4.41.0
peft==0.11.0                    # LoRA adapter loading
jellyfish==1.0.4                # Jaro-Winkler for mock client

# --- Config ---
pyyaml==6.0.1
python-dotenv==1.0.1

# --- Monitoring (Step 7) ---
evidently==0.4.30
prometheus-client==0.20.0

# --- MLflow ---
mlflow==2.13.0

# --- Airflow (DAG only — Airflow itself provided by shared compose) ---
# apache-airflow -- runs on Airflow VM, not in this container


# --- Testing ---
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0                   # TestClient for FastAPI
